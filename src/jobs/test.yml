description: >
  # What will this job do?
  # Descriptions should be short, simple, and clear.
  Sample description

parameters:
  install-gems:
    default: false
    type: boolean

executor: default
steps:
  - checkout
  - build-site:
      install-gems: << parameters.install-gems >>
  - test-site
